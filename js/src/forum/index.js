// Simple JavaScript version of forum index
const app = require('flarum/forum/app');

app.initializers.add('rich-text-editor', function() {
  console.log('Rich Text Editor extension loaded successfully!');
  
  // TODO: Add rich text editor functionality
  // This is the main entry point for the forum-side extension
});
